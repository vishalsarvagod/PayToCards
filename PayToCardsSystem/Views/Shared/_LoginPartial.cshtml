@using Microsoft.AspNet.Identity
@if (Session["UserPersonalDetails"] != null)
{
    using (Html.BeginForm("LogOff", "Login", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            @{
                var userDetails = (user_personal_details)Session["UserPersonalDetails"];
                <li>
                    @*<span class="navbar-brand">Heading</span>*@
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Welcome @userDetails.FirstName <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>@Html.ActionLink("My Profile","MyProfile","MyProfile")</li>                      
                        <li>@Html.ActionLink("Change Password", "ChangePassword", "Login")</li>
                        <li><a href="#">Settings</a></li>
                        <li class="divider"></li>
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>


                    </ul>
                </li>
            }
        </ul>
                    }
                }
